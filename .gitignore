# Python
server/__pycache__/
server/**/__pycache__/
server/.venv/
server/venv/
server/analizador.db

# env files (nunca subir claves)
.env
.env.local
server/.env
web/.env

# cookies exportadas del navegador (nunca subir - son sesion privada)
server/*cookies*.txt

# credenciales de la cuenta de servicio de Google (Feature 2 - auditoria)
server/service-account*.json
server/*credentials*.json

# Node / Vite
web/node_modules/
web/dist/

# Sistema
.DS_Store
Thumbs.db
